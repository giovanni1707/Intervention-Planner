/* ============================================================
   seed.js — Seed disabled. App starts with clean Firestore.
   ============================================================ */

async function seedDemoData() {
  // Seeding disabled — app uses live Firestore data only.
  return;
}
